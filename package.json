{
  "name": "copilot-conference",
  "version": "1.0.0",
  "description": "Two Copilot agents that debate a question and converge on a consensus answer",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --require ts-node/register --test test/**/*.test.ts",
    "check": "npm run test && npm run build",
    "start": "npm run build && caffeinate -i node dist/orchestrator.js",
    "dev": "ts-node src/orchestrator.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "0.21.0",
    "@inquirer/select": "^5.1.5",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "25.6.2",
    "ts-node": "10.9.2",
    "typescript": "6.0.3"
  }
}
